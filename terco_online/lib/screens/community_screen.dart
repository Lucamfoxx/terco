import 'package:flutter/material.dart';

class CommunityScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Comunidade e Interação Social'),
      ),
      body: Center(
        child: Text('Conteúdo da Comunidade e Interação Social...'),
      ),
    );
  }
}
